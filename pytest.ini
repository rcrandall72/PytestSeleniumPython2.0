[pytest]
addopts =
    --html=report.html --self-contained-html
    --json=report.json
    -v

python_files = test_*.py

markers =
    smoke: Smoke Tests
    regression: Regression Tests
    given: BDD marker for "given" steps
    when: BDD marker for "when" steps
    then: BDD marker for "then" steps